# Private Case-Study Overlay

The public repository contains generic methodology, synthetic examples, and reusable tooling. Identifiable production job names, internal paths, PR details, exact savings evidence, and organization-specific pricing belong in a separate private overlay.

Keep the private pack outside public release artifacts and install it only in approved environments. Public documentation and generated examples must remain understandable without it.